# Copy this file to .env.local and fill in the values. Do NOT commit .env.local.
#
# In the BFF model, Google OAuth credentials live on the SERVER (backend/.env),
# not in the browser. The frontend only needs the API base URL.
VITE_API_BASE_URL=http://localhost:8081
